/**
 * Marks every hardcoded / illustrative value in the UI with a small red alert icon so
 * it's obvious what is not live data yet. Set to false to hide the icons everywhere.
 */
export const SHOW_DEMO_ALERTS = true;
