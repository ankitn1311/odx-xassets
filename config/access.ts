/**
 * Whether "Launch App" asks for a password and the app routes require the access cookie.
 * Flip to true to turn the private-access gate back on; both the landing button and the
 * middleware read this.
 */
export const REQUIRE_LAUNCH_PASSWORD = false;
